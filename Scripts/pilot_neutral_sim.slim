// Author: Jonathan Mah
// Date: 20190701
// Purpose: Run 10 neutral simulations, each with 100
//   individuals. For each simulation, output a VCF.

// Set up a simple neutral simulation.
initialize()
{
  // Set the overall mutation rate
  initializeMutationRate(1e-7);

  // m1 mutation type: neutral.
  initializeMutationType("m1", 0.5, "f", 0.0);

  // g1 genomic element type: uses m1 for all mutations.
  initializeGenomicElementType("g1", m1, 1.0);

  // Uniform chromosome of length 100 kb.
  initializeGenomicElement(g1, 0, 99999);

  // Uniform recomination along the chromosome.
  initializeRecombinationRate(1e-8);
}

// create 10 populations, each of 100 individuals
1
{
  // subPopCount = 10;
  // for (i in 1:subPopCount)
  //   sim.addSubpop(i, 100);
  sim.addSubpop('p1', 100);
  sim.addSubpop('p2', 100);
  sim.addSubpop('p3', 100);
  sim.addSubpop('p4', 100);
  sim.addSubpop('p5', 100);
  sim.addSubpop('p6', 100);
  sim.addSubpop('p7', 100);
  sim.addSubpop('p8', 100);
  sim.addSubpop('p9', 100);
  sim.addSubpop('p10', 100);
}

// run to generation 10000
10000
{
  p1.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p2.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p45.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p5.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p9.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p1.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p1.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p1.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p1.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");
  p1.outputVCFSample(100, filePath = "../Data/pilot_neutral_sim/output_9.vcf");

  sim.simulationFinished();
  print('Simulation has ended.');
}
